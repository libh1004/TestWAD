@model IEnumerable<TestWAD.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>ist of fine payers</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fines)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumFines)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Id)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Fines)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.NumFines)
    </td>
</tr>
}
<p>
    @Html.ActionLink("List of fines", "ListFines", "Students")
</p>
    
</table>
